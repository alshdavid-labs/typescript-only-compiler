{
  "scripts": {
    "start": "run-p serve build:watch",
    "serve": "http-server static -s",
    "build": "gulp build",
    "build:watch": "gulp build && gulp watch"
  },
  "devDependencies": {
    "http-server": "^0.12.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.7.4"
  },
  "dependencies": {
    "gulp": "^4.0.2",
    "gulp-typescript": "^6.0.0-alpha.1",
    "merge2": "^1.3.0"
  }
}
