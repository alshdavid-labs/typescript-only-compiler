{
  "scripts": {
    "start": "run-p serve build:watch",
    "serve": "http-server static -s",
    "build": "tsc",
    "build:watch": "tsc -w"
  },
  "devDependencies": {
    "http-server": "^0.12.0",
    "npm-run-all": "^4.1.5",
    "typescript": "^3.7.4"
  }
}
